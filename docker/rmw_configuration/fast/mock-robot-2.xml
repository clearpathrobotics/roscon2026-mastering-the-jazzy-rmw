<?xml version="1.0" encoding="UTF-8" ?>
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">

  <!-- Pin Fast DDS to mock-robot-2's exact mock-b0 address (172.30.10.0/24 is
       the only segment shared with the observer and mock-robot-1). The
       interfaceWhiteList restricts SPDP multicast + all traffic to that NIC,
       so the observer reliably discovers this robot across the star
       topology. Analogous to cyclone/mock-robot-2.xml's <NetworkInterface>. -->
  <transport_descriptors>
    <transport_descriptor>
      <transport_id>udp_mock_b0</transport_id>
      <type>UDPv4</type>
      <interfaceWhiteList>
        <address>172.30.10.12</address>
      </interfaceWhiteList>
    </transport_descriptor>
  </transport_descriptors>

  <participant profile_name="default_participant" is_default_profile="true">
    <rtps>
      <userTransports>
        <transport_id>udp_mock_b0</transport_id>
      </userTransports>
      <useBuiltinTransports>false</useBuiltinTransports>
      <builtin>
        <discovery_config>
          <discoveryProtocol>SIMPLE</discoveryProtocol>
          <EDP>SIMPLE</EDP>
          <leaseDuration>
            <sec>DURATION_INFINITY</sec>
          </leaseDuration>
        </discovery_config>
      </builtin>
    </rtps>
  </participant>

</profiles>
